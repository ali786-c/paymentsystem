---
name: typescript-react
description: "Typescript React for paymentsystem. 3 conventions, 2 fixes."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **48 real patterns** in **paymentsystem**. This skill is auto-routed to agents when working on typescript-react files.

## 🔧 Problem Playbooks

### problem-fix in App.tsx
- // Build: LinkPayPro v1.0.1
+ // Build: LinkPayPro v1.0.2 - Fixed Navigation

📌 IDE AST Context: Modified symbols likely include [App, default]

**Actionable Steps:**
1. Modified 1 files

### Fixed null crash in Authorization — prevents null/undefined runtime crashes
-             await axios.post(`${API_BASE_URL}/admin/merchants/${merchant.id}/configs`, {
+             const url = merchant.id === 'global' 
-                 configs: [
+                 ? `${API_BASE_URL}/admin/merchants/global/save-configs` 
-                     {
+                 : `${API_BASE_URL}/admin/merchants/${merchant.id}/configs`;
-                         gateway_name: 'stripe',
+

**Actionable Steps:**
1. Modified 1 files
2. identifier: Authorization
3. identifier: Bearer
4. identifier: Settings
5. identifier: Failed

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Replaced lucide-react with lucide-react — confirmed 3x** — - import { ArrowRight, CheckCircle2, ShieldCheck, Zap, Layers, Globe, Cpu, BarChart3 } from 'lucide-
- 📐 **Added session cookies authentication — improves module reusability — confirmed 4x** — -         <p>Your data is used solely to facilitate payment processing, ensure compliance with finan
- 📐 **Replaced lucide-react with lucide-react — confirmed 6x** — - import { FileText, Building2, UserCheck, Shield, ArrowRight, CheckCircle } from 'lucide-react';
+ 

## 🔵 Architecture

### how-it-works in Hero.tsx
File updated (external): frontend/src/components/landing/Hero.tsx

Content summary (121 lines):
import React from 'react';
import { ArrowRight, CheckCircle2, ShieldCheck, Zap, Layers } from 'lucide-re

## 🤔 Decisions & Trade-offs

- **Optimized ArrowRight — improves module reusability** — - import { ArrowRight, Zap, Layers, Globe, Cpu, BarChart3 } from 'lucide-react';
+ import { ArrowRig
- **Optimized Gateways — improves module reusability** — -     visible: { opacity: 1, x: 0, transition: { duration: 0.6, ease: "easeOut" } }
+     visible: {
- **Optimized React — improves module reusability** — - import { ArrowRight, CheckCircle2, ShieldCheck, Zap, Layers } from 'lucide-react';
+ import React 
- **decision in Hero.tsx** — -               <button className="w-full sm:w-auto px-10 py-4 bg-slate-900 text-white rounded-2xl f
- **decision in Navbar.tsx** — -           <Link to="/get-started" className="text-sm font-bold text-white bg-blue-600 px-8 py-2.5 

---
*Auto-generated by BrainSync 🧠 | 48 patterns | 2026-03-24*
