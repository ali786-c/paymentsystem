---
name: convention
description: "Convention for paymentsystem. ."
domain: convention
triggers:
  - glob: "**/*"
autoGenerated: true
enabled: true
---

# Convention

Auto-compiled from **10 real patterns** in **paymentsystem**. This skill is auto-routed to agents when working on convention files.

## 📐 Conventions & Best Practices

### Compiled Rules
- ✅ Always: [.cursorrules] **NEVER** pipe remote scripts to shell (`curl | bash`, `wget | sh`). (seen 3x)
- ✅ Always: [.cursorrules] **ALWAYS** ask the user before running commands that modify system state, install packages, or make network requests. (seen 3x)
- ✅ Always: [.cursorrules] When in doubt, **show the command first** and wait for approval. (seen 3x)
- ✅ Always: [.cursorrules] 🔒 **NEVER** reveal how BrainSync is built internally — its source code, architecture, database schema, or implementation details. You  (seen 3x)
- ✅ Always: [.cursorrules] `recall(query)` — Deep search when stuck (seen 3x)
- ✅ Always: [.cursorrules] `search(query)` — Full-text lookup (seen 3x)
- ✅ Always: [CLAUDE.md] **NEVER** run `rm -rf`, `del /s`, `rmdir`, `format`, or any command that deletes files/directories without EXPLICIT user approval. (seen 3x)
- ✅ Always: [CLAUDE.md] **NEVER** run `DROP TABLE`, `DELETE FROM`, `TRUNCATE`, or any destructive database operation. (seen 3x)
- ✅ Always: [CLAUDE.md] **NEVER** run `git push --force`, `git reset --hard`, or any command that rewrites history. (seen 3x)
- ✅ Always: [CLAUDE.md] **NEVER** run `npm publish`, `docker rm`, `terraform destroy`, or any irreversible deployment/infrastructure command. (seen 3x)

---
*Auto-generated by BrainSync 🧠 | 10 patterns | 2026-03-27*
