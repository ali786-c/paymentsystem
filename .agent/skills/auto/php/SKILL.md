---
name: php
description: "Php for paymentsystem. 1 conventions, 2 fixes."
domain: php
triggers:
  - glob: "**/*.php"
autoGenerated: true
enabled: true
---

# Php

Auto-compiled from **14 real patterns** in **paymentsystem**. This skill is auto-routed to agents when working on php files.

## 🔧 Problem Playbooks

### Fixed null crash in LOGIC — prevents null/undefined runtime crashes
-             // Update the invoice with the chosen method
+             // --- 💡 TAX LOGIC START 💡 ---
-             $invoice->update(['payment_method' => $validated['gateway']]);
+             // Keep track of the original "base" amount in the payload to allow reverting
- 
+             $payload = $invoice->payload ?? [];
-             $redirectUrl = $this->paymentService->processInvoice($invo

**Actionable Steps:**
1. Modified 1 files
2. identifier: TAX
3. identifier: LOGIC
4. identifier: START
5. identifier: Keep

### problem-fix in api.php
File updated (external): api/routes/api.php

Content summary (42 lines):
<?php

use Illuminate\Support\Facades\Route;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\AuthController;
use App\Http\Controllers\AdminController;

Route::post('/login', [AuthController::class, 'login']);

Route::prefix('payment')->name('api.payment.')->group(function () {
    Route::get('/success/{in

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in Webhook — prevents null/undefined runtime crashes — confirmed 3x** — -                 
+                 $type = $payload['type'] ?? 'unknown';
-                 // Pri

---
*Auto-generated by BrainSync 🧠 | 14 patterns | 2026-04-01*
